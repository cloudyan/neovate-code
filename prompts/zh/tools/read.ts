// Read工具提示词模板函数

export function createReadPrompt() {
  return `
从本地文件系统读取文件。您可以直接使用此工具访问任何文件。

用法：
- 默认情况下，它从文件开头开始读取最多 2000 行
- 您可以选择指定行偏移量和限制（对于大文件特别有用），但建议不提供这些参数来读取整个文件
- 超过 2000 个字符的任何行将被截断
- 此工具允许读取图像（例如 PNG、JPG 等）。读取图像文件时，内容会以视觉方式呈现。
  `;
}
